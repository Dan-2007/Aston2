spring:
  application:
    name: emailnotificationservice
  kafka:
    consumer:
      bootstrap-servers: localhost:9092,localhost:9094
      properties:
        spring:
          json:
            trusted:
              packages: "org.example.events"

server:
  port: 8090

logging:
  level:
    root: info